# Pose Module
mediapipe>=0.10.0
opencv-python>=4.8.0
numpy>=1.24.0

# Backend
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0

# MongoDB (replaces SQLite)
motor>=3.3.0          # async MongoDB driver for FastAPI
pymongo>=4.6.0        # sync driver (pulled in by motor)
certifi>=2024.2.2     # TLS CA bundle for Atlas connections

# Utilities
aiofiles>=23.0.0
python-dotenv>=1.0.0
